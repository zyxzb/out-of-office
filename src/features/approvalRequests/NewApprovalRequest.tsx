import { Button } from '../../shadcn/components/ui/button';

const NewApprovalRequest = () => {
  return <Button className='max-w-max'>Add New Request</Button>;
};

export default NewApprovalRequest;
